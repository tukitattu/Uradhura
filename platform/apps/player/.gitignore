*.log
node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo

# Environment (keep the committed example; never commit real URLs/IPs)
.env
.env.local
.env.*.local

# Expo auto-generated env typing (regenerated on `expo start`)
expo-env.d.ts

# Native build output (generated by expo prebuild / EAS in the cloud)
android/
ios/

# Secrets
secrets/
eas-credentials.json