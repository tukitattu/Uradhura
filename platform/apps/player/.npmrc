# This project intentionally mixes expo SDK 57 with react-native 0.73.6, a
# pairing whose peer ranges conflict (RN 0.73 pins react 18.2.0). Do not
# remove — plain `npm install` will otherwise ERESOLVE and fail.
legacy-peer-deps=true